# Environment variables
.env
.env.local
.env.*.local

# Hytale server data (bind mounted into container)
server-data/*
!server-data/.gitkeep

# Server backups (created by update-server.sh)
backups/

# Docker
docker-compose.override.yml

# Claude Code
.claude/
